---
trigger: always_on
---

The frontend and backend use yarn workspaces for dependency management and
script running. Tests and other commands can be run in the system environment,
or in the docker compose service container (app).
